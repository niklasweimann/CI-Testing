name: Build and Test

on:
  push:
    branches:
    - master
    - main
  pull_request:
    branches:
      - master
      - main
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Display GitVersion outputs
      run: echo Build
        
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
    - name: Test
      run: echo test